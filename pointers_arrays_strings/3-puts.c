#include "main.h"
#include ‘<stdio.h>’

/**
 * _puts - function that prints a string, followed by a new line, to stdout.
 * @str:  string to evaluate
 */

void _puts(char *str)
{
	printf("%sc \n", *str);
}
